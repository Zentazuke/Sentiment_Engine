# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Virtual environments ---
.venv/
venv/
env/

# --- Local data / databases (never commit — large, machine-local) ---
*.db
*.db-wal
*.db-shm
recovered.db
sentiment_journal.db
sentiment_journal_snapshot.db
reports.txt

# --- Generated model state ---
calibration_model.json

# --- Transformer / ONNX model artifacts (large) ---
cryptobert_onnx/
*_onnx/
*.onnx

# --- Dashboard: commit the built output so `git pull` ships the UI (like the bot).
#     Ignore the React build tooling (source lives in dashboard/, node_modules is huge).
dashboard/node_modules/
dashboard/dist/
sentiment.env

# --- Secrets / local env ---
.env
*.token
*.secret

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
